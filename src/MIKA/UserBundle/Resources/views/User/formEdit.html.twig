
<div class="well bodycenter">


    {# Les erreurs générales du formulaire. #}
    {{ form_errors(form) }}

    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.email, "form.email", {'label_attr': {'class': 'col-lg-2 col-md-2 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.email) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.username, "form.username", {'label_attr': {'class': 'col-lg-2 col-md-2 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.username) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.username, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>

    <div class="form-group">
        {{ form_label(form.current_password,"form.current_password", {'label_attr': {'class': 'col-lg-2 col-md-2 '}}) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.current_password, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.name, "name", {'label_attr': {'class': 'col-lg-2 col-md-2 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.name) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.name, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.firstname, "firstname", {'label_attr': {'class': 'col-lg-2 col-md-2 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.firstname) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.firstname, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
        <legend>Adresse</legend>
        <div class="form-group">

            {# Génération du label. #}
            {{ form_label(form.streetnumber, "streetnumber",{'label_attr': {'class': 'col-lg-offset-1 col-md-offset-1 col-lg-1 col-md-1 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.streetnumber) }}
            <div class="col-lg-2 col-md-2">
                {# Génération de l'input. #}
                {{ form_widget(form.streetnumber, {'attr': {'class': 'form-control'}}) }}
            </div>
            {# Génération du label. #}
            {{ form_label(form.channeltype,"channeltype", {'label_attr': {'class': 'col-lg-2 col-md-2  control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.channeltype) }}
            <div class="col-lg-2 col-md-2">
                {# Génération de l'input. #}
                {{ form_widget(form.channeltype, {'attr': {'class': 'form-control'}}) }}
            </div>
            {# Génération du label. #}
            {{ form_label(form.street, "street",{'label_attr': {'class': 'col-lg-1 col-md-1 control-label'}}) }}
            {# Affichage des erreurs pour ce champ précis. #}
            {{ form_errors(form.street) }}
            <div class="col-lg-3 col-md-3">
                {# Génération de l'input. #}
                {{ form_widget(form.street, {'attr': {'class': 'form-control'}}) }}
            </div>
        </div>

    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.postalcode, "postalcode", {'label_attr': {'class': 'col-lg-offset-1 col-md-offset-1 col-lg-1 col-md-1 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.postalcode) }}
        <div class="col-lg-2 col-md-2">
            {# Génération de l'input. #}
            {{ form_widget(form.postalcode, {'attr': {'class': 'form-control'}}) }}
        </div>
        {# Génération du label. #}
        {{ form_label(form.city, "city", {'label_attr': {'class': 'col-lg-1 col-md-1 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.city) }}
        <div class="col-lg-3 col-md-3">
            {# Génération de l'input. #}
            {{ form_widget(form.city, {'attr': {'class': 'form-control'}}) }}
        </div>
        {# Génération du label. #}
        {{ form_label(form.country, "country", {'label_attr': {'class': 'col-lg-1 col-md-1 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.country) }}
        <div class="col-lg-3 col-md-3">
            {# Génération de l'input. #}
            {{ form_widget(form.country, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div class="form-group">
        {# Génération du label. #}
        {{ form_label(form.phone, "phone", {'label_attr': {'class': 'col-lg-2 col-md-2 control-label'}}) }}
        {# Affichage des erreurs pour ce champ précis. #}
        {{ form_errors(form.phone) }}
        <div class="col-lg-10 col-md-10">
            {# Génération de l'input. #}
            {{ form_widget(form.phone, {'attr': {'class': 'form-control'}}) }}
        </div>
    </div>
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>

</div>