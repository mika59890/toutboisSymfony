<form action="{{ path('mika_platform_researchProduct') }}" method="post" class="navbar-form form-search pull-right">
    {{ form_widget(form.research) }}
    <button type="submit" class="btn">Rechercher</button>
    {{ form_widget(form) }}
</form>